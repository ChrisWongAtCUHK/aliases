#!/bin/sh

# macOS High Sierra

# specific alias
alias now='export NOW=$(date +%Y%m%d%H%M%S) && echo $NOW'     # digit only datetime
alias today='export TODAY=$(date +%Y%m%d) && echo $TODAY'     # digit only date
